import type { Metadata } from 'next'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'NextGrow | Premium Real Estate',
  description: 'NextGrow Real Estate — Residential & Commercial properties. Building dreams, delivering excellence.',
  keywords: 'NextGrow, real estate, residential, commercial, property, India',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
